# Copy to .env and set values for local emulator.
# For production, use: firebase functions:secrets:set YM_API_KEY
YM_API_KEY=your-api-key-here
# Honor Guard proxy (same value as VITE_APPS_SCRIPT_URL in project root .env)
APPS_SCRIPT_URL=
